# Server Configuration
PORT=3000
NODE_ENV=development

# Redis (optional - if not provided, in-memory storage will be used)
REDIS_URL=redis://localhost:6379

# PostgreSQL Connection (default)
PGHOST=localhost
PGUSER=postgres
PGDATABASE=postgres
PGPASSWORD=password
PGPORT=5432 